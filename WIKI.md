# WIKI.md — LLM Wiki Schema

> 本文件是LLM操作wiki的权威参考文档。
> 基于Andrej Karpathy的LLM Wiki模式，适配为滴滴网约车安全治理方向技术负责人的工作知识库。

---

## 1 — 三层架构

```
vault/
├── 0. 待办与计划/          ┐
├── 1. 目标管理/            │
├── 2. 业务支持/            │ Layer 1: 源文件（只读）
├── 3. 技术建设/            │ vault中除wiki/外的所有目录
├── 4. 团队管理/            │
├── 5. 工作记录/            │
├── 7. 乱七八糟/            │
├── 8. 工具箱/              │
├── 9. Resources/          ┘
│
├── wiki/                   # Layer 2: LLM生成的知识库
│   ├── index.md            # 全量页面索引
│   ├── log.md              # 操作日志（时间倒序）
│   ├── hot.md              # Hot Cache: 近期上下文摘要（~500字）
│   ├── overview.md         # 全局概览
│   ├── 人物/               # 团队成员、候选人、stakeholder画像
│   │   └── _index.md
│   ├── 组织/               # 团队结构、能力分布、资源配置
│   │   └── _index.md
│   ├── 项目/               # 在跑项目、专项、OKR
│   │   └── _index.md
│   ├── 系统/               # 技术系统、架构、依赖关系
│   │   └── _index.md
│   ├── 业务/               # 安全/治理领域知识、规则、流程
│   │   └── _index.md
│   ├── 概念/               # 技术框架、管理方法论
│   │   └── _index.md
│   ├── 事件/               # 线上故障、复盘、关键事件
│   │   └── _index.md
│   ├── 决策/               # 技术选型、组织调整、优先级取舍
│   │   └── _index.md
│   ├── 目标/               # OKR、关键指标、里程碑
│   │   └── _index.md
│   ├── 流程/               # Runbook、审批链路、应急响应
│   │   └── _index.md
│   └── meta/               # lint报告、dashboard
│
└── WIKI.md                 # Layer 3: 本文件（schema定义）
```

### 权限规则

- **源文件目录**（`wiki/`以外所有目录）：只读，修改需用户确认。
- **`wiki/`目录**：LLM拥有完全读写权限，可自由创建、更新、重命名、删除。
- 每个wiki页面必须有frontmatter，无例外。
- 使用`[[wikilink]]`而非markdown路径链接。
- 一页一主题，覆盖两个主题时拆分。
- 已有页面做更新，不要重复创建。

---

## 2 — Hot Cache

`wiki/hot.md`是一份约500字的近期上下文摘要。其他项目引用本vault时，读这个文件就能快速获取最新状态，无需遍历整个wiki。

**更新时机**：每次ingest之后、每次重要的query交互之后、每次会话结束时。

格式：

```markdown
---
type: meta
title: "Hot Cache"
updated: 2026-05-06T14:30:00
---

# 近期上下文

## 最后更新
2026-05-06 — 摄入3份会议纪要

## 关键事实
- [最重要的近期信息]
- [第二重要的信息]

## 近期变更
- 新建：[[张伟]], [[新老链路切换]]
- 更新：[[策略平台]]（补充架构依赖）
- 标记：[[安全数据团队]]与[[治理数据团队]]在数据口径上存在矛盾

## 活跃线索
- 当前在推进[某事项]
- 待解决：[某个开放问题]
```

控制在500字以内。这是缓存，不是日志。每次全量覆写。

---

## 3 — Frontmatter Schema

每个wiki页面以扁平YAML frontmatter开头。不使用嵌套对象（Obsidian属性面板不支持）。

### 通用字段（每页必填）

```yaml
---
type: <人物|组织|项目|系统|业务|概念|事件|决策|目标|流程|meta>
title: "页面标题"
created: 2026-05-06
updated: 2026-05-06
tags:
  - <领域标签>
  - <类型标签>
status: <种子|发展中|成熟|常青>
related:
  - "[[关联页面]]"
sources:
  - "[[源文件路径]]"
---
```

### 按类型扩展字段

**人物**：`role`（职位）、`team`（所属团队）、`level`（职级）、`location`（base城市）、`risk`（稳定性风险，高/中/低）、`first_mentioned`

**组织**：`org_type`（本部/部门/小组/虚拟组织）、`headcount`、`leader`、`parent_org`

**项目**：`project_type`（OKR/专项/日常）、`owner`、`priority`（P0-P3）、`start_date`、`target_date`、`progress`（0-100）

**系统**：`system_type`（平台/服务/中间件/工具）、`owner_team`、`tech_stack`、`dependencies`（wikilink列表）、`stability`（高/中/低）

**业务**：`domain`（安全/治理）、`scope`、`aliases`（别名列表）

**概念**：`complexity`（基础/进阶/高级）、`domain`、`aliases`

**事件**：`event_type`（线上故障/复盘/组织变动/里程碑）、`severity`（P0-P3）、`date`、`impact`、`owner`

**决策**：`decision_type`（技术选型/组织调整/优先级取舍/架构变更）、`decision_date`、`decided_by`、`options`（考虑过的选项列表）、`outcome`、`revisit_date`

**目标**：`goal_type`（OKR/KPI/里程碑）、`owner`、`period`（如2026-Q2）、`target_value`、`current_value`、`unit`、`parent_goal`（wikilink）

**流程**：`process_type`（Runbook/审批/应急/日常）、`owner`、`trigger`（触发条件）、`frequency`（按需/日/周/月）、`last_executed`

---

## 4 — Operations

### 4.1 INGEST — 摄入

触发：用户指定源文件或粘贴内容。

1. 完整阅读源文件。
2. 与用户讨论关键要点。用户说"直接摄入"则跳过。
3. 为源文件中提到的每个人物创建或更新`wiki/人物/`页面。
4. 为提到的团队/组织创建或更新`wiki/组织/`页面。
5. 为提到的项目创建或更新`wiki/项目/`页面。
6. 为提到的技术系统创建或更新`wiki/系统/`页面。
7. 提取业务领域知识到`wiki/业务/`。
8. 提取方法论/框架到`wiki/概念/`。
9. 如有线上故障/关键事件，创建`wiki/事件/`页面。
10. 如有决策记录（选型、取舍），创建`wiki/决策/`页面。
11. 如有目标/指标/KPI，创建或更新`wiki/目标/`页面。
12. 如有流程/Runbook/审批链路，创建`wiki/流程/`页面。
13. 更新各子目录的`_index.md`。
11. 更新`wiki/index.md`，添加所有新页面条目。
12. 更新`wiki/hot.md`。
13. 在`wiki/log.md`顶部追加记录：
    ```markdown
    ## [2026-05-06] ingest | 源文件标题
    - 源文件：`4. 团队管理/1:1沟通/张伟-0506.md`
    - 新建：[[张伟]], [[安全策略架构团队]]
    - 更新：[[班子建设]], [[组织架构]]
    - 要点：一句话概括新增信息。
    ```
14. 检查矛盾信息，用`> [!contradiction]` callout在两个页面上标记。

### 4.2 INGEST — 批量模式

触发：用户指定多个文件或说"把这些都摄入"。

1. 列出所有待处理文件，与用户确认。
2. 逐个按4.1流程处理，暂缓交叉引用。
3. 全部完成后：交叉引用检查，寻找源文件之间的关联。
4. index、hot cache、log统一更新一次。
5. 汇报："处理了N个源文件，新建X页，更新Y页。关键关联：..."

批量摄入交互更少。超过30个文件时，每10个检查点汇报一次。

### 4.3 QUERY — 回答问题

1. 先读`wiki/hot.md`，可能已有答案。
2. 读`wiki/index.md`定位相关页面。
3. 读3-5个相关页面（超过10个太多了）。
4. 在对话中综合回答，用wikilink引用来源。
5. 如果回答质量好，主动建议沉淀为wiki页面。
6. 如果发现知识缺口："关于X的信息不够，要不要找源文件补充？"

### 4.4 LINT — 巡检

触发：用户说"巡检"、"lint"，或每10-20次摄入自动执行。

检查项：
- 孤立页面（无入链）
- 死链（指向不存在的页面）
- 过时信息（updated超过30天）
- 提到但未创建页面的概念/人物
- 缺失的交叉引用
- frontmatter字段缺失
- 空章节

输出：`wiki/meta/lint-report-YYYY-MM-DD.md`。自动修复前先征求用户意见。

---

## 5 — Index与Sub-Index

### wiki/index.md（主索引）

```markdown
---
type: meta
title: "Wiki索引"
updated: 2026-05-06
---
# Wiki索引

## 人物
- [[张伟]] — 安全策略架构负责人（status: 成熟）

## 组织
- [[安全本部]] — 安全侧团队集合（headcount: 45）

## 项目
- [[新老链路切换]] — P0专项（progress: 60%）

## 系统
- [[策略平台]] — 安全策略核心系统

## 业务
- [[乘客安全]] — 乘客侧安全规则与流程

## 概念
- [[AI落地框架]] — 出行技术AI应用方法论

## 事件
- [[0506线上故障]] — P1，策略平台误杀

## 决策
- [[策略引擎选型]] — 2026-03选择自研方案

## 目标
- [[安全事件率]] — 2026-Q2目标：≤0.5‰

## 流程
- [[线上故障响应]] — P0/P1故障应急Runbook
```

### 子目录Sub-Index

每个子目录下的`_index.md`是该类别的局部索引。

```markdown
---
type: meta
title: "人物索引"
updated: 2026-05-06
---
# 人物

## 直属团队负责人
- [[张伟]] — 安全策略架构，base上海
- [[杨晓帆]] — 治理策略

## Stakeholder
- [[可可]] — 出行技术T1
- [[曹乐]] — 网约车技术部T0
```

### wiki/log.md

只追加，不修改历史。新条目在顶部。每条格式：`## [YYYY-MM-DD] operation | 标题`

---

## 6 — 跨项目引用

任何Claude Code项目都可以读取本wiki，无需复制上下文。

在其他项目的CLAUDE.md中添加：

```markdown
## Wiki知识库
路径：~/Documents/Ad-Astra

需要本项目之外的上下文时：
1. 先读wiki/hot.md（近期上下文，~500字）
2. 不够则读wiki/index.md（全量索引）
3. 需要特定领域则读wiki/<子目录>/_index.md
4. 最后才读具体wiki页面

不要为通用编程问题、项目内已有的上下文、或无关领域去读wiki。
```

token开销：hot cache约500 token，index约1000 token，单页100-300 token。

---

## 7 — Context Window管理

读最少必要的内容：

- 先读`hot.md`，可能已经够用。
- 再读`index.md`，定位相关页面，不要全量扫描。
- 用子目录`_index.md`做定向查找。
- 每次query只读3-5个页面，超过10个太多了。
- 用搜索做关键词查找，不要逐页扫描。
- 做局部修改时用PATCH，不要为了改一个字段重写整个文件。
- wiki页面控制在100-300行，过长则拆分。
- 不要把wiki内容贴到对话中，除非用户要求。用wikilink引用。

---

## 8 — 约定

### 命名

- **文件名**：中文标题（`张伟.md`、`新老链路切换.md`、`策略平台.md`）
- **目录名**：中文（`wiki/人物/`、`wiki/系统/`）
- **标签**：中文，层级式（`#安全/策略`、`#治理/在线`）
- **文件名唯一**，wikilink无需带路径

### 页面结构

每个wiki页面的标准结构：

```markdown
---
type: 人物
title: "张伟"
created: 2026-05-06
updated: 2026-05-06
tags:
  - 安全
  - 管理者
status: 成熟
related:
  - "[[安全策略架构团队]]"
sources:
  - "[[4. 团队管理/班子建设追踪]]"
---

# 张伟

> 安全策略架构负责人，目前最靠谱的管理者，家在上海，稳定性存疑。

## 基本信息
...

## 能力评估
...

## 关键事件
...

## 关联
- 团队：[[安全策略架构团队]]
- 上级：[[刘帅]]

## 来源
- [[4. 团队管理/班子建设追踪]]
- [[5. 工作记录/周会纪要-0506]]
```

### 写作风格

- 陈述句、现在时。"X负责Y"而不是"X基本上在做Y"。
- 中西文之间不加空格。
- 大量使用wikilink，每次提到wiki中已有页面时都加链接。
- 标注来源：`（来源：[[页面名]]）`。
- 标记不确定信息：`> [!gap] 此处需要更多信息。`
- 标记矛盾：`> [!contradiction] [[页面A]]说X，但[[页面B]]说Y。`

### 交叉引用

更新页面A提到页面B时，检查页面B是否也应该反向链接到A。双向链接让graph view有用。

---

## 总结

LLM的职责：
1. 从源文件中摄入信息，提取、归类、交叉引用
2. 维护hot cache，每次操作后更新
3. 回答问题：index → 相关页面 → 综合回答
4. 好的回答沉淀为wiki页面
5. 定期巡检，发现并修复健康问题
6. 源文件只读，不得修改
7. 每次操作更新index、sub-index、log、hot cache
8. 所有页面使用frontmatter和wikilink
